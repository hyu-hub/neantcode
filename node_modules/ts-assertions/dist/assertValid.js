import { assert, } from './assert';
export function assertValid(value, message, validator) {
    const isValid = validator || Boolean;
    assert(isValid(value), message);
    return value;
}
//# sourceMappingURL=assertValid.js.map